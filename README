文件的执行顺序：
（1）首先执行BuildTable.py，该表为建表代码,同一个IQQ.db只需执行一次，即如果IQQ.db已存在，则不必执行该文件
（2）然后执行Server.py，此为服务器代码，同一电脑只需执行一次
（3）最后执行Main.py，该文件为客户端代码，可以执行多次，代表多个客户端，如果server部署在远程服务器上，则可以跨主机通信。
    连接服务器需要知道服务器的IP和端口，默认是127.0.0.1，可以在POTOCOL.py里修改，修改成server所在ip即可。
	test